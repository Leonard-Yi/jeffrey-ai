name: Vercel Keepalive

on:
  schedule:
    - cron: '*/5 * * * *'  # Every 5 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Vercel
        run: |
          curl -s -o /dev/null -w "%{http_code}" https://jeffrey-ai.vercel.app/api/health || true
